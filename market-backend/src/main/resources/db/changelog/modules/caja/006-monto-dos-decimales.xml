<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <!-- Ver ventas/009-monto-dos-decimales.xml: mismo cambio, misma razón.
         monto_final_contado es nullable (caja todavía abierta) — "!= ROUND(...)"
         ya excluye los NULL del conteo por semántica SQL normal. -->
    <changeSet id="caja-006-monto-dos-decimales" author="market-backend">
        <preConditions onFail="HALT">
            <sqlCheck expectedResult="0">
                SELECT count(*) FROM caja_sesion WHERE monto_inicial != ROUND(monto_inicial, 2)
                    OR monto_final_contado != ROUND(monto_final_contado, 2)
            </sqlCheck>
            <sqlCheck expectedResult="0">
                SELECT count(*) FROM movimiento_caja WHERE monto != ROUND(monto, 2)
            </sqlCheck>
        </preConditions>
        <sql>
            ALTER TABLE caja_sesion ALTER COLUMN monto_inicial TYPE NUMERIC(12,2);
            ALTER TABLE caja_sesion ALTER COLUMN monto_final_contado TYPE NUMERIC(12,2);
            ALTER TABLE movimiento_caja ALTER COLUMN monto TYPE NUMERIC(12,2);
        </sql>
        <rollback>
            <sql>
                ALTER TABLE caja_sesion ALTER COLUMN monto_inicial TYPE NUMERIC(12,4);
                ALTER TABLE caja_sesion ALTER COLUMN monto_final_contado TYPE NUMERIC(12,4);
                ALTER TABLE movimiento_caja ALTER COLUMN monto TYPE NUMERIC(12,4);
            </sql>
        </rollback>
    </changeSet>
</databaseChangeLog>
