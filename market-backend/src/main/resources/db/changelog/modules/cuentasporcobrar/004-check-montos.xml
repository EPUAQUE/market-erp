<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <!-- Fase 3 del plan (PLAN_MEJORAS.md): defensa en profundidad para invariantes
         que el dominio ya exige (CuentaPorCobrar/Cobro), ahora también en BD.
         saldo_pendiente <= monto_original es una invariante adicional (nunca puede
         quedar debiendo más de lo que se generó originalmente) que el dominio
         mantiene por construcción pero que no tenía respaldo en BD. -->
    <changeSet id="cuentasporcobrar-004-check-montos" author="market-backend">
        <sql>
            ALTER TABLE cuenta_por_cobrar ADD CONSTRAINT ck_cuenta_por_cobrar_monto_original CHECK (monto_original > 0);
            ALTER TABLE cuenta_por_cobrar ADD CONSTRAINT ck_cuenta_por_cobrar_saldo_pendiente CHECK (saldo_pendiente >= 0);
            ALTER TABLE cuenta_por_cobrar ADD CONSTRAINT ck_cuenta_por_cobrar_saldo_le_original CHECK (saldo_pendiente &lt;= monto_original);
            ALTER TABLE cobro ADD CONSTRAINT ck_cobro_monto CHECK (monto > 0);
        </sql>
        <rollback>
            <sql>
                ALTER TABLE cuenta_por_cobrar DROP CONSTRAINT ck_cuenta_por_cobrar_monto_original;
                ALTER TABLE cuenta_por_cobrar DROP CONSTRAINT ck_cuenta_por_cobrar_saldo_pendiente;
                ALTER TABLE cuenta_por_cobrar DROP CONSTRAINT ck_cuenta_por_cobrar_saldo_le_original;
                ALTER TABLE cobro DROP CONSTRAINT ck_cobro_monto;
            </sql>
        </rollback>
    </changeSet>
</databaseChangeLog>
