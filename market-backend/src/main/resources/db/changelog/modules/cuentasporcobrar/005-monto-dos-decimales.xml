<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <!-- Ver ventas/009-monto-dos-decimales.xml: mismo cambio, misma razón. -->
    <changeSet id="cuentasporcobrar-005-monto-dos-decimales" author="market-backend">
        <preConditions onFail="HALT">
            <sqlCheck expectedResult="0">
                SELECT count(*) FROM cuenta_por_cobrar WHERE monto_original != ROUND(monto_original, 2)
                    OR saldo_pendiente != ROUND(saldo_pendiente, 2)
            </sqlCheck>
            <sqlCheck expectedResult="0">
                SELECT count(*) FROM cobro WHERE monto != ROUND(monto, 2)
            </sqlCheck>
        </preConditions>
        <sql>
            ALTER TABLE cuenta_por_cobrar ALTER COLUMN monto_original TYPE NUMERIC(12,2);
            ALTER TABLE cuenta_por_cobrar ALTER COLUMN saldo_pendiente TYPE NUMERIC(12,2);
            ALTER TABLE cobro ALTER COLUMN monto TYPE NUMERIC(12,2);
        </sql>
        <rollback>
            <sql>
                ALTER TABLE cuenta_por_cobrar ALTER COLUMN monto_original TYPE NUMERIC(12,4);
                ALTER TABLE cuenta_por_cobrar ALTER COLUMN saldo_pendiente TYPE NUMERIC(12,4);
                ALTER TABLE cobro ALTER COLUMN monto TYPE NUMERIC(12,4);
            </sql>
        </rollback>
    </changeSet>
</databaseChangeLog>
