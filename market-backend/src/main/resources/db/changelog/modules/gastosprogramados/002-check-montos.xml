<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <!-- Fase 3 del plan (PLAN_MEJORAS.md): defensa en profundidad para invariantes
         que el dominio ya exige (GastoProgramado/PagoGastoProgramado), ahora
         también en BD. -->
    <changeSet id="gastosprogramados-002-check-montos" author="market-backend">
        <sql>
            ALTER TABLE gasto_programado ADD CONSTRAINT ck_gasto_programado_monto CHECK (monto > 0);
            ALTER TABLE pago_gasto_programado ADD CONSTRAINT ck_pago_gasto_programado_monto CHECK (monto > 0);
        </sql>
        <rollback>
            <sql>
                ALTER TABLE gasto_programado DROP CONSTRAINT ck_gasto_programado_monto;
                ALTER TABLE pago_gasto_programado DROP CONSTRAINT ck_pago_gasto_programado_monto;
            </sql>
        </rollback>
    </changeSet>
</databaseChangeLog>
