<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <!-- Ver ventas/009-monto-dos-decimales.xml: mismo cambio, misma razón. -->
    <changeSet id="gastosprogramados-003-monto-dos-decimales" author="market-backend">
        <preConditions onFail="HALT">
            <sqlCheck expectedResult="0">
                SELECT count(*) FROM gasto_programado WHERE monto != ROUND(monto, 2)
            </sqlCheck>
            <sqlCheck expectedResult="0">
                SELECT count(*) FROM pago_gasto_programado WHERE monto != ROUND(monto, 2)
            </sqlCheck>
        </preConditions>
        <sql>
            ALTER TABLE gasto_programado ALTER COLUMN monto TYPE NUMERIC(12,2);
            ALTER TABLE pago_gasto_programado ALTER COLUMN monto TYPE NUMERIC(12,2);
        </sql>
        <rollback>
            <sql>
                ALTER TABLE gasto_programado ALTER COLUMN monto TYPE NUMERIC(12,4);
                ALTER TABLE pago_gasto_programado ALTER COLUMN monto TYPE NUMERIC(12,4);
            </sql>
        </rollback>
    </changeSet>
</databaseChangeLog>
