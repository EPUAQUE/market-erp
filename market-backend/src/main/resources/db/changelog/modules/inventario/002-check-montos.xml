<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <!-- Fase 3 del plan (PLAN_MEJORAS.md): defensa en profundidad para invariantes
         que el dominio ya exige (Inventario/MovimientoInventario), ahora también en BD. -->
    <changeSet id="inventario-002-check-montos" author="market-backend">
        <sql>
            ALTER TABLE inventario ADD CONSTRAINT ck_inventario_existencia CHECK (existencia_actual >= 0);
            ALTER TABLE inventario ADD CONSTRAINT ck_inventario_costo_promedio CHECK (costo_promedio_actual >= 0);
            ALTER TABLE movimiento_inventario ADD CONSTRAINT ck_movimiento_inventario_cantidad CHECK (cantidad > 0);
            ALTER TABLE movimiento_inventario ADD CONSTRAINT ck_movimiento_inventario_costo_unitario CHECK (costo_unitario >= 0);
        </sql>
        <rollback>
            <sql>
                ALTER TABLE inventario DROP CONSTRAINT ck_inventario_existencia;
                ALTER TABLE inventario DROP CONSTRAINT ck_inventario_costo_promedio;
                ALTER TABLE movimiento_inventario DROP CONSTRAINT ck_movimiento_inventario_cantidad;
                ALTER TABLE movimiento_inventario DROP CONSTRAINT ck_movimiento_inventario_costo_unitario;
            </sql>
        </rollback>
    </changeSet>
</databaseChangeLog>
