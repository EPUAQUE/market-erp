<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <!-- Ver ventas/008-cantidad-entera.xml: mismo cambio, misma razón (unidades
         enteras, sin productos por peso/volumen). existencia_actual es el acumulado
         de estas mismas cantidades (Inventario.aplicar), así que se ajusta junto con
         movimiento_inventario.cantidad. -->
    <changeSet id="inventario-003-cantidad-entera" author="market-backend">
        <preConditions onFail="HALT">
            <sqlCheck expectedResult="0">
                SELECT count(*) FROM movimiento_inventario WHERE cantidad != TRUNC(cantidad)
            </sqlCheck>
            <sqlCheck expectedResult="0">
                SELECT count(*) FROM inventario WHERE existencia_actual != TRUNC(existencia_actual)
            </sqlCheck>
        </preConditions>
        <sql>
            ALTER TABLE movimiento_inventario ALTER COLUMN cantidad TYPE NUMERIC(12,0);
            ALTER TABLE inventario ALTER COLUMN existencia_actual TYPE NUMERIC(12,0);
        </sql>
        <rollback>
            <sql>
                ALTER TABLE movimiento_inventario ALTER COLUMN cantidad TYPE NUMERIC(12,3);
                ALTER TABLE inventario ALTER COLUMN existencia_actual TYPE NUMERIC(12,3);
            </sql>
        </rollback>
    </changeSet>
</databaseChangeLog>
